# ─────────────────────────────────────────────────────────────────
#  Pro Stock Suite — BACKEND requirements
#  Deploy as a separate Render Web Service
#  Python 3.11 recommended
# ─────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Data & finance
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.2.0
feedparser>=6.0.11

# ML & forecasting
scikit-learn>=1.4.2
xgboost>=2.0.3
statsmodels>=0.14.2
prophet>=1.1.5

# GARCH volatility model (optional but recommended)
arch>=6.3.0

# SARIMA auto-order (optional but recommended)
pmdarima>=2.0.4

# News sentiment — uses DistilBERT (downloads model on first run ~260 MB)
# NOTE: pulls in PyTorch CPU automatically via transformers
transformers>=4.40.0
torch>=2.2.0     # CPU-only; removes CUDA bloat on free tier

# ─── NOT included (too heavy for Render free 512 MB RAM) ───────────
# tensorflow  →  LSTM models will be disabled (TF_AVAILABLE=False)
#               Install manually if you upgrade to a paid Render plan
